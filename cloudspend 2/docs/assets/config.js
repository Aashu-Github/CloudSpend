window.CLOUDSPEND_CONFIG = {
  // Render Blueprint uses this service name by default. If Render assigns a
  // different URL, update this one value before publishing GitHub Pages.
  productionApiBaseUrl: 'https://cloudspend-api.onrender.com',
  localApiBaseUrl: 'http://127.0.0.1:8080',
  maxUploadMb: 50
};
